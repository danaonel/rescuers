## Packages
framer-motion | Smooth animations for page elements and scroll reveals

## Notes
- Site content is fetched from a single endpoint: /api/content
- Images are provided as URLs in the gallery array
- Social icons need to be mapped dynamically from the platform string
